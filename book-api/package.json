{
  "name": "book-api",
  "version": "1.0.0",
  "description": "Mock API for book search",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database.json --port 3000 --host 0.0.0.0 & prism mock openapi.yaml -p 4010 --host 0.0.0.0 & redoc-cli serve openapi.yaml --host 0.0.0.0 --port 8080"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "@stoplight/prism-cli": "^5.5.2",
    "redoc-cli": "^0.13.21"
  }
}
