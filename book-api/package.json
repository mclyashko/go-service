{
  "name": "book-api",
  "version": "1.0.0",
  "description": "Mock API for book search",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
